repos:
  - repo: local
    hooks:
      - id: update-frontmatter-dates
        name: Update markdown front matter dates (date/updated)
        entry: python3 scripts/update_frontmatter_dates.py
        language: system
        files: ^docs/.*\.md$
